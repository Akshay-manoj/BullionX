version: '3.8'

services:
  customer-web:
    build:
      context: ./apps/customer-web
      dockerfile: Dockerfile
    container_name: bullionx-customer-web
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
